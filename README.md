### Hello Node App

Objectives :

* Create VM with Vagrant
* Install docker-ce with ansible via Vagrant
* Build specific image and run stand-alone container with ansible via Vagrant
    
```
$ vagrant up
$ vagrant ssh
```
    
To test:
```
curl -i localhost:8084
```

